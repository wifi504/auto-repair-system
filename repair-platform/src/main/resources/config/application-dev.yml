server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/auto_repair_system
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource

cors:
  # 允许哪些源访问
  allowed-origins: http://localhost:5173/

minio:
  # MinIO 服务地址
  endpoint: http://localhost:9000/
  # 访问密钥
  access-key: repair-sys-oss
  secret-key: r5DzwPtp7fSTGzUZVRS7

logging:
  level:
    com.lhl.rp: debug